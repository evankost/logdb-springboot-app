package com.example.LogDB.jwt.token;

public enum JwtTokenType {                                                      ////Enum representing JWT type, which is of Bearer authentication type.
  BEARER
}
